# アーキテクチャ設計書: {アプリ名}

- 作成日: {YYYY-MM-DD}
- 入力: docs/02_requirements.md, docs/03_ui_design.md

## 1. 技術スタック

| 領域 | 採用技術 | 選定理由 |
|---|---|---|
| フレームワーク | {例: Vite + React + TypeScript} | {CLAUDE.md既定値 or 変更理由} |
| スタイル | {例: プレーンCSS + CSS変数} | |
| データ永続化 | {例: localStorage} | |
| テスト | Vitest + Playwright | |

## 2. ディレクトリ構成

```
app/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── styles/
│   │   └── tokens.css        ← デザイントークン(docs/03_ui_design.md から)
│   ├── components/
│   │   └── {Component}.tsx
│   ├── lib/                  ← 純関数ロジック(ユニットテスト対象)
│   │   └── {logic}.ts
│   └── types.ts
└── e2e/
    └── {flow}.spec.ts
```

## 3. データモデル

```ts
{型定義をそのまま記載。ストレージのスキーマバージョンも含める}
```

- 永続化方式: {どこに・いつ保存するか}
- 壊れたデータ・旧バージョンデータの扱い: {必須}

## 4. コンポーネント構成と責務

| コンポーネント | 責務 | 対応画面(SC-ID) |
|---|---|---|
| {App} | {ルーティング・全体状態} | - |
| {XxxForm} | {入力とバリデーション} | SC-001 |

- 状態管理の方針: {どの状態をどこで持つか}
- ロジックとUIの分離方針: {lib/ に置く純関数の一覧}

## 5. 実装タスク分割(実装順)

| # | タスク | 内容 | 完了条件 | 対応要件 |
|---|---|---|---|---|
| 1 | 雛形+トークン | プロジェクト初期化、デザイントークンCSS変数、フォント読み込み | ビルドが通りトークンが反映 | - |
| 2 | {データ層} | {型定義+永続化+純関数} | ロジックのユニットテスト可能 | FR-xxx |
| 3 | {画面1} | | ビルドが通り画面が表示 | FR-xxx |
| N | 仕上げ | 空状態・エラー状態・レスポンシブ・モーション | design-guidelines セルフチェック通過 | NFR-xxx |

## 6. 既知の課題

{実装中・レビューで発生した未解決事項をここに追記していく}
